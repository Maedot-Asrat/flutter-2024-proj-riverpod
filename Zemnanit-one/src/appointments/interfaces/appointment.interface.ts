export interface Appointment {
    id?: string;
    time: string;
    date?: string;
    hairstyle?: string; 
    comment?: string;
     // If relevant for appointments
    // Add other appointment-specific properties
}
